# مجموعهٔ اجارهٔ دنیای اقتصاد — شهریور ۱۴۰۲، منطقهٔ ۲۲

خروجی `scripts/parse_donyae_eqtesad_listing_tables.py --table shahrivar-1402-district-22`.

- **منبع:** خوان روزنامه دنیای اقتصاد، شمارهٔ خبر ۳۹۹۷۸۳۱
- **تاریخ:** ۸ شهریور ۱۴۰۲ → `1402-06`
- **پوشش:** فقط منطقهٔ ۲۲
- **ورودی:** `data/raw/donya-e-eqtesad_rent-asking_shahrivar-1402_district-22_cleaned.md`
  (نسخهٔ پاک‌سازی‌شدهٔ مالک) و فایل جایگزین‌شدهٔ
  `donya-e-eqtesad_rent-asking_shahrivar-1402_district-22.md`

## چرا این تاریخ مهم است

`1402-06` داخل بازهٔ سری فروش Kilid (`1400-06` تا `1405-05`) می‌افتد. پیش از افزودن این جدول،
تنها اجارهٔ سطح‌منطقه در کل آن بازه دو جدول ۱۴۰۴ بود.

| | |
|---|---|
| ردیف | ۴۳ |
| ردیف با اجارهٔ قابل‌استفاده | ۱۷ |
| ردیف با «-» به‌جای اجاره | ۲۵ |
| منطقه | ۲۲ |
| میانهٔ اجاره | ۱۵٬۰۰۰٬۰۰۰ تومان در ماه |
| میانهٔ اجاره بر متر مربع | ۸۸٬۸۸۹ تومان |

## سلول‌های مشکوک

| ردیف | چه چیزی | برخورد |
|---|---|---|
| `شهرک راه آهن - بلوار کوهک` (۱۰۷ م²) | اجارهٔ «۱ میلیارد» — برابر با ودیعهٔ همان ردیف و ۶۷ برابر میانهٔ جدول | `rent_equals_deposit_suspected_duplication` — ردیف می‌ماند، اجاره خالی |
| `هفت آسیاب` و `همت غرب - دریاچه` | ودیعه «۱ میلیارد و ۱۱۰۰ میلیون» | `amount_ambiguous_doubled_digit` — خالی، با دو خوانش ممکن |
| `دهکده المپیک` و `بلوار کوهک` | ودیعه «۱ میلیارد و ۱۲۰۰ میلیون» | همان |
| ۲۵ ردیف | «-» به‌جای اجاره | `rent_not_listed_in_source` — خالی، نه صفر |

هر دو خوانشِ ودیعهٔ مبهم در `parse_report.json` زیر `ambiguous_deposits` آمده است:
مجموع تحت‌اللفظی (۲٬۱۰۰ و ۲٬۲۰۰ میلیون) یا بازنویسی همان مبلغ (۱٬۱۰۰ و ۱٬۲۰۰ میلیون).
نکتهٔ مهم: **همین رشته‌ها در هر دو نسخهٔ جدول عینا تکرار شده‌اند**، پس این یک اشکال سمت منبع است،
نه خطای بازنویسی مالک. در همان جدول، `چیتگر` با شکل استاندارد «دو میلیارد و ۲۰۰ میلیون» نوشته
شده، پس هر دو خوانش باز می‌ماند.

## تفاوت با نسخهٔ اول (reconciliation)

نسخهٔ دوم جدول، نسخهٔ اول را جایگزین می‌کند. تفاوت‌ها ماشینی محاسبه و در
`parse_report.json` زیر `reconciliation` ثبت شده‌اند:

| | |
|---|---|
| نسخهٔ اول | ۴۵ ردیف |
| نسخهٔ دوم (پاک‌سازی‌شده) | ۴۳ ردیف |
| تطبیق‌یافته | ۴۲ |
| نام عوض‌شده | ۴۱ (نرمال‌سازی نام محله) |
| **فقط در نسخهٔ دوم** | ۱ — `آبشار` (۱۱۰ م²، ۴ میلیون، ۵۵۰ میلیون) |
| **حذف‌شده از نسخهٔ دوم** | ۳ — `کوهسار` (۱۲۵ م²، ۴ میلیون، ۴۰۰ میلیون)، `همت غرب، بلوار درسا` (۱۲۰ م²، -، ۵۵۰ میلیون)، `افشین اردستانی` (۱۱۴ م²، -، ۱ میلیارد) |
| تغییر مقدار در ردیف‌های تطبیق‌یافته | **۰** — هیچ عددی بازنویسی نشده، فقط نام‌ها و مجموعهٔ ردیف‌ها |

سه ردیف حذف‌شده در `excluded/superseded_rows_shahrivar-1402-district-22.csv` نگه داشته شده‌اند
تا هیچ ردیفی بی‌سند ناپدید نشود.

### دو پرسش باز

1. `آبشار` و `سرو آزاد` هر دو با ۱۱۰ م²، ۴ میلیون اجاره و ۵۵۰ میلیون ودیعه ثبت شده‌اند. یا دو
   آگهی واقعا یکسان‌اند، یا یک آگهی زیر دو نام محله آمده است.
2. سه ردیف حذف‌شده در نسخهٔ پاک‌سازی‌شده به‌عمد کنار گذاشته شده‌اند یا از قلم افتاده‌اند؟

## فایل‌ها

| فایل | نقش |
|---|---|
| `rent_asking.csv` | ۴۳ ردیف، ۲۸ ستون |
| `parse_report.json` | شمارش، میانه‌ها، دامنه‌ها، سلول‌های مشکوک، reconciliation، پرسش‌های باز |
